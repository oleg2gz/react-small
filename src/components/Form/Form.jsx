export const Form = () => {
  return (
    <form>
      <label>Add Color Name</label>

      <input type="text" placeholder="Add color name" required />

      <button type="button">Toggle color</button>
    </form>
  )
}
